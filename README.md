# PROJECT-LUX Mockups

This orphan branch stores design mockups only.

- Branch: `codex/mockups`
- Implementation source of truth: `main`
- Do not treat files on this branch as a code snapshot.
- Do not add Source, Content, Config, Plugins, project binaries, or implementation assets here.
- New mockup topics belong under `Docs/WIP/<topic>/`.
- This branch is not merged wholesale into `main`. Confirmed decisions are transferred deliberately.
